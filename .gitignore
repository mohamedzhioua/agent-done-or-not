# Proof receipts are local runtime artifacts, never committed.
.agent-proof/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp

# Internal handoff — never committed
HANDOFF.md

# npm auth — never commit a token-bearing .npmrc
.npmrc

# codex-rescue runtime artifacts
.ai-os/

# External feedback — never committed (tracked separately)
feedbacks/
