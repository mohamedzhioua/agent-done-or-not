- id: agent-done-assert
  name: Agent done-or-not assert
  description: Block the commit unless a fresh passing proof-of-done receipt exists in .agent-proof/.
  entry: hooks/pre-commit-assert.sh
  language: script
  pass_filenames: false
  always_run: true
  stages: [pre-commit]
